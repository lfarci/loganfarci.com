# Required until @tailwindcss/vite releases a version whose peerDependencies
# include Vite 8 (currently declares "^5.2.0 || ^6 || ^7"). Remove once the
# upstream package.json is updated.
legacy-peer-deps=true
